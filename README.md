# ObesityResearchNE
 Analysis of correlations between obesity, mortality, and comorbilities. 

 To run, make sure to first install all relevant imports (will add a list/dockerfile later), then hit run all on code2.ipynb (that contains all of the relevant code, code.ipynb only contains very early code that is no longer useful
 ).
